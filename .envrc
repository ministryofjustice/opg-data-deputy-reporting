# Export API Version
export REST_API_VERSION=$(ls -d lambda_functions/*/ | awk -F'/' '{print $2}' | grep '^v[1-9]\+' | sort -r | head -n1)
export MOCKING_ENV='host.docker.internal'
./.circleci/setup.sh
