---
version: '3'

services:
  mock-digideps:
    volumes:
      - ./mock_digideps/openapi.yml:/app/openapi.yml
  mock-lambda:
    volumes:
      - ./lambda_functions/v2/functions/documents/app:/var/task/app
  mock-sirius:
    volumes:
      - ./mock_sirius/openapi.yml:/app/openapi.yml
  unit-tests:
    volumes:
      - ./lambda_functions/v2/functions/documents/app:/var/task/app
  localstack:
    volumes:
      - ./lambda_functions/v2/functions/csv-forwarder/main.zip:/tmp/localstack/main.zip
